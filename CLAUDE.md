# CLAUDE.md

このリポジトリで作業する際の方針。

## プロジェクト概要

複数のLLMにリバーシを対戦させ、モデル間の強さを比較するアプリケーション。実装のほとんどをAIに書かせて開発を進める(AI駆動開発の実践プロジェクト)。

詳細な設計方針は [docs/](docs/) を参照。engine実装時に読むべきファイル・web実装時に読むべきファイルは[docs/README.md](docs/README.md)に整理してある。ドキュメントを更新した場合は必ずこのファイルとの整合を確認すること。

## 目的の要点(誤解しやすいポイント)

- **リバーシを打つアルゴリズム(強いAI)を作ることが目的ではない。**
- リバーシのルール部分(合法手の列挙・石の反転・勝敗判定など)のみアルゴリズムで実装する。
- **どこに石を置くかの意思決定は外部LLMに行わせる。** 探索や評価関数によるゲームAIは実装しない。

## 用語

- 「オセロ」は商標のため使用しない。必ず「リバーシ」と表記する。

## リポジトリ構成

- `engine/`: 対戦エンジン(Python)。ローカル実行。盤面管理・LLM呼び出し・リーグ運営・棋譜出力。
- `web/`: 可視化用フロントエンド。GitHub Pagesでデプロイ可能な静的サイトとして構成する。
- `data/`: `engine/`が書き出し`web/`が読み込む棋譜JSON・リーグ結果の受け渡し場所。engine/webどちらの所有物でもない中立フォルダ。**リポジトリにそのままコミットする(gitignoreしない)**。engineはローカル実行のみでCI上では再生成できないため、GitHub Pages公開に使うデータはコミット済みである必要がある。ハイライト対局選定のようなキュレーションは行わず、全量を公開対象とする。
- `docs/`: 設計・仕様ドキュメント。`shared/`(engine・web共通)・`engine/`・`web/`に分けている(詳細は[docs/README.md](docs/README.md)参照)。

`engine/` と `web/` はコードレベルで互いに依存させない。両者のやり取りは必ず `data/` のJSON経由で行う。

## よく使うコマンド

```sh
# engine
cd engine
uv run pytest                                        # 単体テスト(tests/live/はキー未設定ならskip)
uv run reversi-engine run-league --dry-run           # 実行予定カードの確認(APIキー不要)
uv run reversi-engine run-league                     # 未実施カードの対局を実行
uv run reversi-engine aggregate                      # results.jsonl -> ranking.json
uv run python tests/generate_dummy_data.py --reset    # data/にダミー対局を生成

# web
cd web
npm run dev      # data/のコピー(predev)込みで開発サーバ起動
npm run build    # 型チェック + 本番ビルド
```

## 開発方針

- `engine/` の依存関係管理は `uv` を使用する。
- LLM呼び出しの抽象化にLangChain等のフレームワークは使わず、薄い自作Adapter層で行う。
- `web/` はビルドツールチェイン(Vite等)を使用し、GitHub Pagesにデプロイ可能な静的ファイルとして完結させる(サーバーサイド処理を持たない)。

## 進め方・コミット方針

- 設計や仕様は、ユーザーとの相談を通じて`docs/`・`CLAUDE.md`に随時反映していく。各ファイルの内容が「その時点で合意済みの事項」であり、各ドキュメント内の「未決定事項」節が「まだ決まっていないこと」を示す。会話(チャット)が途切れても、これらのファイルを読めば経緯を再構成できる想定。
- 決定の理由は、決定ログのような別ファイルにまとめるのではなく、**各ドキュメント本文に直接書く**(例: `docs/shared/log-schema.md`の「既存フォーマットとの関係」節)。
- 議論が一区切りついたタイミング(1ドキュメントの内容が固まった、方針が1つ決まった、など)で**都度git commitする**。コミット単位を細かく保つことで、`git log`自体が意思決定の履歴になる。
