# 対戦ルール

## 盤面・基本ルール

- 8x8の標準的なリバーシ盤、初期配置も標準(中央4マスに石を斜め配置)。
- 黒(先手)・白(後手)が交互に着手する。
- 合法手が存在しない場合は自動的にパスする(LLMに問い合わせない)。
- 両者連続でパスした場合、終局とする。
- 盤面が埋まった場合も終局とする。
- 終局時、石数が多い方の勝ち。同数の場合は引き分け。

## 1手ごとの処理

1. 現在の盤面・合法手一覧をLLMに提示し、着手をJSON形式で応答させる。
2. 応答のJSONパースに失敗した場合、**1回だけ**同一プロンプトで再試行する。再試行も失敗した場合は反則負けとする。
3. パース成功後、応答内容が合法手一覧に含まれない場合は反則負けとする(リトライしない)。
4. 1手あたりの最大待ち時間(タイムアウト)を超えた場合も反則負けとする。

- 反則負けとなった対局は「不成立」として除外するのではなく、**反則負けとして記録し集計に含める**(モデルの安定性を測る指標になるため)。

既存のオセロ棋譜フォーマット(WTHOR/GGF)にはLLM対戦特有の反則負け・リトライという概念自体が存在しないため、これらの扱いは独自に設計する([docs/log-schema.md#既存フォーマットとの関係](log-schema.md#既存フォーマットとの関係)参照)。

## 未決定事項

- [ ] 1手あたりのタイムアウト秒数(デフォルト値の検討が必要)
- [ ] リトライ時のプロンプトを変更するか(パースエラー内容をLLMにフィードバックするか)
- [ ] 反則負けの理由(タイムアウト/非合法手/パース失敗)を棋譜に区別して記録するか

## リーグ運営

- 出場モデルの全組み合わせで総当たり戦を行う。
- 同一カード(モデルA vs モデルB)は先手後手を入れ替えて複数回対戦する(先手・後手1回ずつが最小単位)。
- 対戦回数は設定値とする(例: 先手後手2セットずつ=計4局)。

## 未決定事項(リーグ)

- [ ] 同一カードあたりの対戦回数
- [ ] 勝点方式(勝ち=1点/引き分け=0.5点 等)かELOレーティングか、あるいは両方併記するか
